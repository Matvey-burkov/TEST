#include <iostream>
#include <fstream>

using namespace std;

int main(){

    ifstream file("text.txt");
    string s;
    int x = 0;

    while(getline(file,s)){
        x++;
    }
    cout<<x;

    return 0;
}

